# calc module notes

- Provides tiny calculator helpers used by the demo CLI.
- `percent(part, total)` returns `part` as a percentage of `total`.
- `running_total(values)` returns a running (cumulative) sum for a list of values.
- Pure functions with no side effects, operating on plain floats.

Reviewed-by: intentlab-ai-dev[bot]

