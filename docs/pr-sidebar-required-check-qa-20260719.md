# PR Sidebar Required Check QA — refreshed 2026-07-20

Temporary fixture for verifying required-check rendering in the PR sidebar.

This fixture ships a workflow with two jobs — one intentionally failing
(required) and one passing (optional) — so the dashboard's required-check
acceptance can be observed on a live PR. Safe to delete after QA.

Refresh marker: dashboard `v0.0.0-dev.29725298152`.
