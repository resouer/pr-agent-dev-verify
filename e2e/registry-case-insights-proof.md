# Registry Case Insights — Proof

This file is a synthetic end-to-end artifact. Its purpose is to verify that a
mixed-case repository owner (e.g. `CaseOwner` vs `caseowner`) still resolves to
the same registry entry.

All names and examples here are synthetic and used only to confirm mixed-case
repository resolution.

A case-folded lookup for `CaseRepo` reaches the same canonical repository as `caserepo`.
