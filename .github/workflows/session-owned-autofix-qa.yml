name: Session-owned Auto-fix QA

on:
  pull_request:

permissions:
  contents: read

jobs:
  session-owned-auto-fix-qa:
    name: Session-owned Auto-fix QA
    runs-on: ubuntu-latest
    steps:
      - name: Deterministic fixture result
        run: exit 1
